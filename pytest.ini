[tool:pytest]
DJANGO_SETTINGS_MODULE = ai_task_system.settings
python_files = tests.py test_*.py *_tests.py
addopts = -v --tb=short --strict-markers --ds=ai_task_system.settings
markers =
    integration: marks tests as integration tests
    unit: marks tests as unit tests